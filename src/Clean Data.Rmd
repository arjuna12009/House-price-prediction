---
title: "Cleaning data"
author: "Arjuna Anilkumar, A20446963"
date: "11/8/2020"
output: pdf_document
---



# Clean data 

### Install packages

```{r}
#install.packages(c("Amelia","purrr","tidyr","ggplot2"))
```

### Load data

```{r}
df <- read.table("../data/raw/train.csv", sep = ",",header = T)
head(df)
df2 <- read.table("../data/raw/test.csv", sep = ",",header = T)
head(df2)
train_data <- df
test_data <- df2
```

### Missing values

```{r}
library(Amelia)
misscounts <- sapply(train_data,function(x) sum(is.na(x)))
missmap(train_data, main = "Missing values")
sum(misscounts)
dim(train_data)

misscounts <- sapply(test_data,function(x) sum(is.na(x)))
missmap(test_data, main = "Missing values")
sum(misscounts)
dim(test_data)
```


# Explore data

### Summarize data

```{r}
summary(train_data)
```

```{r}
summary(as.factor(train_data$MSZoning))
summary(as.factor(test_data$MSZoning))
```

### Numerical variables histogram

```{r}
library(purrr)
library(tidyr)
library(ggplot2)
train_data %>%
  keep(is.numeric) %>% 
  gather() %>% 
  ggplot(aes(value)) + 
      facet_wrap(~ key, scales = "free") + 
      geom_histogram(aes(y=..density..), fill = "grey") + 
      geom_density()
```


